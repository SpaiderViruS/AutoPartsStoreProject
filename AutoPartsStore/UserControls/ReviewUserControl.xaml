<UserControl x:Class="AutoPartsStore.UserControls.ReviewUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoPartsStore.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal">
            <Label x:Name="RaitingLabel" FontSize="16"/>
            <Label x:Name="NameUser" />
        </StackPanel>

        <Image x:Name="UserImage" Grid.Row="1" Width="100" Height="100" Margin="5"/>

        <Label x:Name="ReviewDescriptionLabel" Grid.Row="1" Grid.Column="1"/>
    </Grid>
</UserControl>
